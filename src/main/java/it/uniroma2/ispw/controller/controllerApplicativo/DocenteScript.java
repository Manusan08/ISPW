package it.uniroma2.ispw.controller.controllerApplicativo;

public class DocenteScript {
  /*  public class AulaController {
        private AulaModel model;
        private AulaView view;

        public AulaController(AulaModel model, AulaView view) {
            this.model = model;
            this.view = view;
        }

        public void avviaRicerca() {
            view.mostraFiltri();
            // Codice per prendere i filtri scelti dall'utente
            FiltroRicerca filtri = new FiltroRicerca();
            // Imposta i filtri selezionati
            List<AulaBean> classiDisponibili = model.cercaClassi(filtri);
            view.mostraClassiDisponibili(classiDisponibili);
        }

        public void scegliClasse(AulaBean aula) {
            model.setAulaSelezionata(aula);
            view.mostraFormPrenotazione();
            // Codice per prendere i dati di prenotazione
            Prenotazione prenotazione = new Prenotazione();
            prenotazione.setAula(aula);
            // Imposta i dati di prenotazione
            view.confermaPrenotazione();
        }

        public void confermaPrenotazione(Prenotazione prenotazione) {
            model.prenotaAula(prenotazione);
            view.mostraMessaggio("Prenotazione confermata");
            view.mostraPaginaPrincipale();
        }
    }*/

}
